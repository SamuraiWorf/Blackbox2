@using blackbox2_blink

@if map1.getPlayersInside().length() == 0 && map2.getPlayersInside().length() == 0 && map3.getPlayersInside().length() == 0
    @var reset()
@fi

@var blackbox2::playRandomSong(player)